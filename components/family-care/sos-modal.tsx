export function SosModal({ person, sent, onSend, onClose }: { person: string; sent: boolean; onSend: () => void; onClose: () => void }) {
  return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="sos-modal" role="dialog" aria-modal="true" aria-labelledby="sos-title" onMouseDown={(event) => event.stopPropagation()}><button className="close-modal" type="button" onClick={onClose} aria-label="Cerrar">×</button><div className="sos-mark">SOS</div><p className="eyebrow">MODO DE SIMULACIÓN</p><h2 id="sos-title">Alerta familiar de emergencia</h2><p>Se notificará a los contactos autorizados de <strong>{person}</strong>. Esta prueba no realiza llamadas reales.</p><div className="sos-contacts"><span className="avatar-group"><i>JT</i><i>MC</i><i>+1</i></span><span>3 contactos configurados</span></div>{sent ? <div className="test-success" role="status">✓ Simulación enviada correctamente</div> : <button className="send-sos" type="button" onClick={onSend}>Mantener para enviar prueba</button>}<button className="cancel-button" type="button" onClick={onClose}>Cancelar</button></section></div>;
}
